spring.application.name=finmate_smartmoneymanagement_be
# Load local .env when running in IDE/CLI from backend or repo root
spring.config.import=optional:file:.env[.properties],optional:file:../.env[.properties]

#database
spring.datasource.url=jdbc:mysql://gateway01.ap-southeast-1.prod.aws.tidbcloud.com:4000/finmate_db?sslMode=VERIFY_IDENTITY&useSSL=true&serverTimezone=UTC
spring.datasource.username=43Z6onyhX92aZky.root
spring.datasource.password=${DB_PASSWORD}


#config
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Cloudinary
cloudinary.cloud-name=root
cloudinary.api-key=${CLOUD_API_KEY}
cloudinary.api-secret=${CLOUD_API_SECRET}

# Swagger
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method

app.jwt.secret=${JWT_SECRET:finmate-super-secret-key-change-me-please-1234567890}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# Google Sign-In
google.oauth.allowed-client-ids=${GOOGLE_ALLOWED_CLIENT_IDS:}

# Mail (OTP / Password Reset)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
app.otp.expiry-minutes=10
